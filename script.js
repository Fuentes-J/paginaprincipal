document.addEventListener('DOMContentLoaded', function() {
    const mapElement = document.getElementById('map');

    // Iniciando un mapa básico (puedes usar Google Maps o cualquier otra API de mapas)
    // Aquí se usa un mapa básico de ejemplo sin funcionalidad real.
    mapElement.innerHTML = '<p>Mapa de ubicación (aquí podría ir un mapa interactivo)</p>';
});
